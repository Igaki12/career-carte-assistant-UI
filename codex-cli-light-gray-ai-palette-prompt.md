# Codex CLI Prompt: 暗色UIから白・薄グレー・ニュートラルグレー配色へ置き換える


画面デザインの配色だけを変更してください。
現在の黒基調・夜を想起させる暗色UIを、白・薄グレー・ニュートラルグレーを基調とした明るいUIへ置き換えてください。

重要:
- 機能、ルーティング、認証、保存キー、保存形式、データ型、文言、表示/非表示条件は変更しないでください。
- レイアウト、余白、角丸、水平線、カード構造、ボタン配置、モーダル幅、テーブル構造は変更しないでください。
- 既存の上端ライン、影、境界線、透明パネル構造は残し、色だけを差し替えてください。
- 既存の user/company-admin/operations-admin/consultant/admin などの画面導線は変更しないでください。
- GitHub Pages 配信用に `npm run build` で生成される `docs` も更新してください。
- 未追跡ファイルや今回の作業に関係ないファイルは触らないでください。
- 特に `chatgpt-5.4_official_prompt_guidance.md` が存在していても今回の対象外です。

最終的な配色方針:
- ページ背景は黒/濃紺ではなく、かなり薄いグレー系のグラデーションにしてください。
- 本文・見出し・主要アクセント・境界線・主要ボタンは、ニュートラルグレー系を基調にしてください。
- 水平ラインは、濃いグレー系を採用してください。
- 影も、ニュートラルグレー系を採用してください。
- パネル、入力欄、モーダル、テーブル面は薄いニュートラルグレー系にしてください。
- 主要ボタンだけは濃いニュートラルグレーのグラデーションで画面を締めてください。
- 黒や濃紺の大面積背景は残さないでください。
- 青緑や青系アクセントを主配色として残さないでください。状態意味を持つ色だけは必要最小限で維持してください。

推奨カラートークン:
- ページ背景:
  - `linear(135deg, #ffffff 0%, #f7f7f8 52%, #eeeeef 100%)`
  - body/root のフォールバック背景: `#f7f7f8`
- パネル/カード/透明領域:
  - 通常パネル: `rgba(250, 250, 251, 0.78)`
  - 強いパネル: `rgba(252, 252, 253, 0.94)`
  - 薄いパネル/テーブル面: `rgba(244, 244, 245, 0.86)`
- 入力欄:
  - `#fcfcfd`
- モーダル:
  - モーダル外枠: `rgba(252, 252, 253, 0.98)`
  - モーダルヘッダー/フッター: `linear-gradient(135deg, rgba(252, 252, 253, 0.98), rgba(244, 244, 245, 0.96))`
  - モーダル本文面: `rgba(244, 244, 245, 0.90)`
- 本文色:
  - メイン本文: `#27272a`
  - 補助本文: `#52525b`
  - サブテキスト/placeholder: `#71717a`
- 境界線:
  - 通常: `rgba(82, 82, 91, 0.26)`
  - 強め: `rgba(63, 63, 70, 0.42)`
  - 薄め: `rgba(82, 82, 91, 0.18)`
- スクロールバー:
  - トラック: `rgba(82, 82, 91, 0.18)`
  - つまみ: `rgba(82, 82, 91, 0.34)`
  - つまみ hover: `rgba(63, 63, 70, 0.52)`
- 水平ライン:
  - メインライン: `linear(to-r, transparent, rgba(75, 85, 99, 0.58), rgba(31, 41, 55, 0.74), transparent)`
  - サブライン: `linear(to-r, transparent, rgba(156, 163, 175, 0.52), rgba(75, 85, 99, 0.46), transparent)`
- 影:
  - 通常: `0 24px 70px rgba(63, 63, 70, 0.14)`
  - モーダル: `0 32px 96px rgba(39, 39, 42, 0.20)`
- PrimaryButton:
  - 通常: `linear(115deg, #27272a 0%, #3f3f46 24%, #52525b 48%, #3f3f46 72%, #27272a 100%)`
  - disabled: `linear(115deg, rgba(82, 82, 91, 0.62), rgba(113, 113, 122, 0.56), rgba(161, 161, 170, 0.48))`

色の置換ルール:
- `#0f172a`, `#111827`, `#1e293b`, `#334155`, `#475569`, `#64748b` などの黒/濃紺/スレート系の大面積背景を、上記の薄グレー背景または薄グレー面色へ置き換えてください。
- `whiteAlpha` 前提の暗色ガラスUIは、薄グレーのライトUIへ置き換えてください。
- `blackAlpha` 前提の影やオーバーレイは、ニュートラルグレー系の rgba へ置き換えてください。
- Chakra の `gray.*` を機械的にすべて消す必要はありませんが、黒基調や夜っぽい大面積色、または画面全体を薄グレー以外に見せる色は置き換えてください。
- 状態意味を持つ `green`, `red`, `orange`, `purple` などは、成功/警告/エラー/状態表示として必要最小限なら維持してください。`blue`, `cyan`, `teal` は主配色や通常操作色として残さず、原則として `gray` に置き換えてください。
- 未作成、未設定、disabled、テーブル背景、フォーム背景などで使う gray 系は、上記の薄グレー面色またはニュートラルグレーの薄い境界線に寄せてください。
- `::-webkit-scrollbar-track`, `::-webkit-scrollbar-thumb`, `::-webkit-scrollbar-thumb:hover` などのスクロールバー配色も確認し、`rgba(59, 130, 246, ...)` のような青系が残っていればニュートラルグレー系へ置き換えてください。

対象ファイル:
少なくとも以下を確認し、必要に応じて色だけを修正してください。
- `src/lib/lightThemeTokens.ts`
- `src/components/PrimaryButton.tsx`
- `src/components/AuthNavigation.tsx`
- `src/components/KartePanel.tsx`
- `src/components/MeetingRoom.tsx`
- `src/components/VrmStage.tsx`
- `src/components/ProcessingIndicator.tsx`
- `src/components/ApiKeyModal.tsx`
- `src/components/SurveyRadar.tsx`
- `src/lib/surveyRadar.ts`
- `src/pages/Login.tsx`
- `src/pages/AdminLogin.tsx`
- `src/pages/UserHome.tsx`
- `src/pages/ConsultantHome.tsx`
- `src/pages/Admin.tsx`
- `src/pages/CompanyAdminHome.tsx`
- `src/pages/OperationsAdminHome.tsx`
- `src/pages/DemographicsSetup.tsx`
- `src/pages/ConditionCheck.tsx`
- `src/App.tsx`
- `src/theme.ts`
- `src/index.css`

画面別の期待値:
- Login / AdminLogin:
  - 黒背景ではなく、薄グレー背景。
  - 入力欄は `#fcfcfd` 近辺。
  - ロゴ/見出し/本文はニュートラルグレー系で読める。
  - ログインボタンは濃いニュートラルグレーグラデーション。
- UserHome / ConsultantHome:
  - ヒーロー画像の上に暗色オーバーレイを置かない。
  - 画像を読みやすくするオーバーレイは薄グレー系にする。
  - 面談/カルテ/アカウント情報などのパネル面は薄グレー系。
  - 文字、境界線、主要ボタンはニュートラルグレー系。
  - 上端ライン/水平ラインは濃いグレー系。
- Admin / CompanyAdminHome / OperationsAdminHome:
  - 管理テーブル、検索入力、通知一覧、企業/ユーザー管理パネルの背景は薄グレー系。
  - テーブルの境界線・ヘッダー・操作ボタンはニュートラルグレー基調で読みやすく。
  - 黒/濃紺のパネルや暗色フォームを残さない。
- DemographicsSetup:
  - 背景アニメーションや大きな背景面は薄グレー系。
  - 読取専用スタイルや入力欄は機能を変えず、色だけ調整。
- MeetingRoom:
  - ルーム外側背景は薄グレー系。
  - チャット領域や入力欄は薄グレー/白寄り。
  - 3D/VRM表示の背景画像やモデル設定ロジックには触らない。
  - 下部操作行、ボタン配置、カルテ確認機能は変更しない。
- AuthNavigation:
  - 暗色ガラスではなく薄グレーのライトパネルへ。
  - 展開/縮小、framer-motion、reduced-motion 対応は維持。
- KartePanel / 主要モーダル:
  - 暗色ガラスではなく、薄グレー面とニュートラルグレー文字へ。
  - モーダル幅、ボタン配置、編集/保存/出力機能は変更しない。

PrimaryButton の注意:
- 濃いグレー/黒グラデーションを、黒くなりすぎない濃いニュートラルグレーグラデーションへ変更してください。
- disabled または準備中状態では、既存のアニメーション停止条件を維持してください。
- disabled 表現は押せない状態が分かる静的な見た目を維持してください。

変更してはいけないもの:
- ルーティング、認証ロジック、保存キー、localStorage/sessionStorage の仕様。
- `demo-accounts.json` のデータ構造。
- `src/lib/karteExport.ts` のPDF/印刷出力の白紙背景や出力書式。ただし画面UIで直接表示される色だけは必要に応じて確認してください。
- VRMモデル、音声、AI API、面談進行ロジック。
- テキスト文言。
- 水平線の有無、カード/パネルの有無、余白、角丸、最大幅。

実装手順:
1. まず `rg` で黒/濃紺/スレート/whiteAlpha/blackAlpha/暗色ガラス系の色指定を洗い出してください。
2. 既存の共通配色トークンがあれば、そこから先に上記の最終配色へ置き換えてください。
3. 共通トークンで拾えない直接指定の色だけ、各ファイルで個別に置き換えてください。
4. 機械置換を使う場合は、置換後に `rgba(..., )` のような壊れた色指定が残っていないか必ず検索してください。
5. 画面構造・文言・機能変更が差分に混ざっていないか確認してください。
6. `npm run build` で `docs` を更新してください。

実装後に必ず実行:
- `npm run lint`
- `npm run build`
- `git diff --check`

可能ならブラウザ確認:
HashRouter 構成の場合は `/#/...` 形式で確認してください。
- `/#/login`
- `/#/admin/login`
- `/#/user`
- `/#/user/demographics`
- `/#/admin`
- `/#/company-admin`
- `/#/operations-admin`
- `/#/consultant`
- `/#/app/initial`

ブラウザ確認の観点:
- 黒/濃紺の大面積背景が残っていない。
- ページ背景はかなり薄いグレー系になっている。
- パネル/入力欄/モーダルの面色は薄いグレー系になっている。
- 本文・見出し・境界線・主要ボタンはニュートラルグレー基調で読みやすい。
- スクロールバーのトラックとつまみはニュートラルグレー系になっており、青系が残っていない。
- 水平ライン/上端ラインは濃いグレー系になっている。
- 影はニュートラルグレー系になっている。
- 水平線、余白、角丸、カード構造、導線、機能、文言が変わっていない。
- disabled 状態、テーブル、フォーム、モーダル、ナビゲーション、カルテ表示が読みやすい。
- コンソールエラーが出ていない。

最後に報告すること:
- 変更した主なファイル。
- 適用した主要な配色値。
- 実行した検証コマンドと結果。
- ブラウザ確認した画面と結果。
- `docs` が更新されたこと。
- 変更しなかったもの、または意図的に維持した意味色があれば簡潔に説明してください。
