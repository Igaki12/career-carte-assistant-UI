
調査結果として、企業導入向けの権限管理は「企業管理者」というロール名だけを増やすよりも、企業をテナントとして扱い、`tenantId` / `organizationId` によって参照可能なユーザー・カルテ・オプション機能を必ず絞り込む形が適している。一般的にも、マルチテナントSaaSではテナント境界を認証済みセッションに紐づけ、全データアクセスでテナント所有権を検証することが重要とされている。単純なRBACだけでは企業ごとの機能差や対象ユーザー範囲を表現しづらいため、本プロジェクトでは「基本ロール + 企業属性 + 機能フラグ」の組み合わせを採用する。

参考:
- OWASP Authorization Cheat Sheet: RBACだけでなく、属性ベース・関係ベースの制御を検討すること、最小権限・デフォルト拒否・リクエストごとの権限検証を推奨。
- OWASP Multi-Tenant Application Security Cheat Sheet: テナントIDをクライアント入力として信用せず、認証済みセッションから導出すること、全クエリ・キャッシュ・ストレージに `tenant_id` を含めることを推奨。
- GDPR Article 9: 顔・健康状態に近いデータは慎重な扱いが必要。日本向け運用でも、同意、利用目的、保存範囲、企業側の閲覧可否を仕様に明記してから実装する。

---

### ミーティング議事録・実装方針（2026-04-10整理）

#### 1. デモグラフィック（プロフィール）入力フローの改善
*   **現状確認**:
    *   `UserHome` からプロフィール入力・編集画面 `(/user/demographics?returnTo=/user)` へ遷移する導線はすでに実装済み。
    *   `UserHome` の上部プロフィールメモと、アカウント情報確認エリアの両方に「プロフィールを編集」ボタンがある。
    *   `InitialMeetingRoom` ではプロフィール未設定時に `/user/demographics?returnTo=/app/initial` へリダイレクトしている。
    *   つまり「ユーザーダッシュボードから変更できるようにする」は実装済みとして扱う。
*   **残課題**:
    *   デモや検証時に、プロフィール登録を必須にすると面談開始までの操作が増える。
    *   ただし本番運用では、プロフィールはカルテ品質と企業別管理に必要なため、完全に不要化するのではなく「デモ用スキップ」として分ける。
*   **決定事項 / 推奨実装**:
    *   `DemographicsSetup` に「デモ用にスキップして進む」ボタンを追加する。
    *   スキップ時は空のプロフィールを保存するのではなく、`demographicsSkipped: true` のような状態を `localStorage` 側に持たせる。
    *   面談開始時は `hasConfiguredDemographics` または `demographicsSkipped` のどちらかを満たせば遷移を許可する。
    *   `UserHome` では「プロフィール未設定（デモスキップ中）」の badge を表示し、後から正式入力できるようにする。
    *   本番バックエンド移行時は、企業管理者が社員アカウント作成時に最低限の会社・部署・利用枠だけを付与し、本人が初回ログイン後に詳細プロフィールを補完する形が自然。

#### 2. B2B運用に向けた「企業管理ロール」の新設
*   **背景**:
    *   現在のアプリには `/admin` のシステム管理コンソールがあり、ユーザーアカウントとコンサルタントアカウントの一覧、追加、編集、CSV一括追加のUIが存在する。
    *   ただし現状はデモ用のフロント状態であり、「企業ごとの管理者が自社社員だけを管理する」ためのテナント境界、認証、権限検証は未実装。
*   **結論**:
    *   「完全管理者」と同じAdmin画面を企業担当者へ見せるのではなく、企業管理者専用の管理範囲を持つ `CompanyAdminHome` を追加するのがふさわしい。
    *   ルーティング案は `/company-admin`。将来的なVPS + Express + DB化では、ログインユーザーの `role` と `tenantId` から表示範囲を決める。
*   **推奨ロール設計**:
    *   `system_admin`: 全企業、全ユーザー、企業契約、機能フラグ、監査ログを管理できる。
    *   `company_admin`: 自社テナント内の社員アカウント作成・停止・利用回数付与・CSV追加ができる。別企業のデータは閲覧不可。
    *   `consultant`: 担当ユーザーのカルテ閲覧・修正、AI練習面談を利用できる。
    *   `user`: 自分の面談、プロフィール、カルテ、アンケートを利用できる。
*   **推奨データモデル**:
    *   `organizations`: `id`, `name`, `status`, `plan`, `enabledFeatures`, `createdAt`
    *   `users`: `id`, `organizationId`, `role`, `name`, `email`, `status`, `initialInterviewLimit`, `continuousInterviewLimit`
    *   `karteRecords`: `id`, `organizationId`, `userId`, `data`, `createdAt`
    *   `featureFlags`: `organizationId`, `stressAnalysisEnabled`, `turnTakingEnabled`, `lightThemeEnabled`
    *   `auditLogs`: `organizationId`, `actorUserId`, `action`, `targetType`, `targetId`, `createdAt`
*   **実装順序**:
    *   1. フロントデモ段階では `src/lib/demoUserState.ts` とは別に、企業・ロール・機能フラグ用のデモ状態を追加する。
    *   2. `Admin` はシステム管理者用として維持し、企業一覧と企業別オプション管理を追加する。
    *   3. `/company-admin` を新設し、`Admin` のユーザー管理UIを再利用しつつ、対象を自社ユーザーだけに限定する。
    *   4. バックエンド移行時に、全APIで `organizationId` をサーバー側セッションから導出し、リクエストパラメータの企業IDを信用しない。
    *   5. 企業管理者の操作は `auditLogs` に必ず残す。

#### 3. ストレス度合いスコアのカルテ反映と企業ごとのオプション化
*   **背景**:
    *   顔の表情・ランドマークから算出するストレス・緊張度合いの機能を、キャリア面談システムに統合する。
    *   企業契約によって利用可否が変わる可能性があるため、企業別のオプション機能として扱う。
*   **導入方針**:
    *   機能名はユーザー向けには「ストレス分析」よりも「緊張度チェック」または「面談コンディションチェック」とする。医療的・診断的な表現は避ける。
    *   入口は `UserHome` に「面談前コンディションチェック」カードを追加するのが最も分かりやすい。
    *   面談画面に常時カメラ分析を混ぜるより、まずは面談前または面談後に別画面で測定し、結果だけをカルテに紐づける。これにより、面談中UIの複雑化と同意取得の曖昧さを避けられる。
    *   ルーティング案は `/user/condition-check`。企業オプションがOFFの場合はカード自体を非表示、または「契約オプション未有効」と表示する。
*   **表示方針**:
    *   `UserHome`: 直近の緊張度スコア、測定日時、測定済み/未測定の状態を表示する。
    *   `KartePanel`: カルテ完成度やプロフィール表示の近くに「面談時コンディション」として、スコア、レベル、測定日時を表示する。
    *   `ConsultantHome`: 担当者が閲覧するカルテ詳細に、ユーザー本人の同意がある場合のみスコアを表示する。
    *   `CompanyAdminHome`: 個人別の細かい顔分析結果は原則表示しない。企業管理者には利用状況や測定完了率などの集計値に留める。
*   **保存方針**:
    *   `conditionRecords`: `id`, `organizationId`, `userId`, `score`, `level`, `measuredAt`, `source`, `consentVersion`
    *   `latestKarte.conditionSummary`: 最新スコア、レベル、測定日時だけを保持する。
    *   生の顔画像・動画は保存しない。デモ段階ではスコアのみ `localStorage` に保存する。
*   **同意・説明**:
    *   測定前に「この機能は表情から面談前後の緊張傾向を参考値として表示するもので、医療・心理診断ではない」ことを明示する。
    *   企業管理者へ個人のスコアを見せるかどうかは、初期実装ではOFFにする。
    *   本番導入前に、保存対象、閲覧者、保存期間、削除方法を仕様に追加する。

#### 4. 表情分析アプリのスマホUI修正の確認
*   **状況**:
    *   スマホ利用時に、説明テキストを読むためにスクロールするとカメラ映像が見えなくなる問題は、フロントエンド側で修正済みと報告されている。
*   **今後の扱い**:
    *   キャリアカルテ側へ統合する際は、スマホでも「説明」「同意」「カメラ」「測定開始ボタン」が同一画面内で把握できるレイアウトにする。
    *   カメラ映像は固定表示または画面上部に残し、測定中に主要UIがスクロールで隠れないようにする。

#### 5. その他（アカデミック・研究関連の共有）
*   顔のランドマークを用いた感情・嘘・ストレス定量化について、論文発表や研究費獲得を見据えた共有があった。
*   プロダクト側では研究用途と企業利用用途を混同しない。研究用データ収集を行う場合は、通常の面談機能とは別の同意文・保存先・利用目的で管理する。

---

### 次のステップ：ToDoリスト

**■ UI / UX・フロントエンド改修**
*   [x] `UserHome` からプロフィール入力・編集画面への導線があることを確認済み。
*   [x] プロフィール登録済みの場合に初回面談へデータ連携される導線があることを確認済み。
*   [ ] `DemographicsSetup` に「デモ用にスキップして進む」ボタンを追加する。
*   [ ] スキップ状態を `localStorage` に保存し、`UserHome` に「プロフィール未設定（デモスキップ中）」badge を表示する。
*   [ ] `UserHome` に「面談前コンディションチェック」カードを追加する。
*   [ ] `/user/condition-check` を追加し、顔分析アプリの測定UIを統合する入口を作る。
*   [ ] UIテーマにおいて、既存のダークトーンに加え、ライトトーンの表示対応を検討する。

**■ バックエンド / 権限管理・カルテ改修**
*   [ ] 企業テナントを表す `organizations` と、企業別機能フラグ `featureFlags` の仕様を定義する。
*   [ ] 既存 `/admin` はシステム管理者用として維持し、企業一覧・企業別オプション管理を追加する。
*   [ ] `/company-admin` を新設し、企業管理者が自社ユーザーだけを追加・停止・CSV登録・利用回数変更できるUIを追加する。
*   [ ] 企業管理者の権限は `role=company_admin` だけでなく `organizationId` で必ずスコープする。
*   [ ] バックエンド化時は、すべてのユーザー・カルテ・監査ログ・ストレス測定結果に `organizationId` を持たせる。
*   [ ] 企業管理者の操作ログを `auditLogs` として保存する。
*   [ ] 電子カルテの画面UIに「面談時コンディション」欄を追加し、最新の緊張度スコア・レベル・測定日時を表示する。
*   [ ] ストレス・緊張度機能を企業単位でON/OFFできるようにする。
*   [ ] 個人のスコアを企業管理者に表示するかどうかは初期実装ではOFFとし、まずは集計表示のみを検討する。
