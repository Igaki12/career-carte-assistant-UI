ご指摘ありがとうございます。PDFの仕様書および打ち合わせ内容に基づき、電子カルテ（SHIRP形式）の項目をより忠実に書き起こして反映しました。

以下がアップデートされた新しい `AGENTS.md` です。

```markdown
# Career Karte Assistant – エージェント設計書

## 1. プロジェクトのゴールと開発方針
現在の単一機能Reactアプリを、将来的な拡張性とVPS運用を見据えた **役割別 SPA (Single Page Application)** 構成へリファクタリングし、統合的なキャリアコンサルタントシミュレータを構築する。
日本型事前問診のスロット充填（初回面談）と、海外アンビエントAIの書記体験（継続面談）を融合したUI/UXを目指し、面談のフェーズに応じた最適な対話制御を提供する。

## 2. アプリケーションアーキテクチャ（SPA構成）
### 2.1 技術スタック
*   **Frontend:** React (Vite), Chakra UI, Three.js + @pixiv/three-vrm
*   **Routing:** React Router DOM (v6)
*   **Hosting (Current):** GitHub Pages (Static /docs) + HashRouter
*   **Hosting (Future):** VPS (Ubuntu) + Node.js (Express) + Apache2 (Reverse Proxy) + DB + BrowserRouter

### 2.2 役割別ページ構成とルーティング
ランディングページを入り口とし、権限ごとのダッシュボードへルーティングする。初回面談と継続面談は完全に別ページとして分離する。
*   **Home (`/`)**: サービス紹介、各ロールへの遷移。
*   **UserHome (`/user`)**: 一般ユーザー用。初回/継続面談のスタート、継続面談のモード選択、カルテ閲覧・出力、アンケート実施機能。
*   **ConsultantHome (`/consultant`)**: コンサルタント用。担当ユーザーのカルテ閲覧・修正、AI練習面談機能（ロードマップ）。
*   **Admin (`/admin`)**: 管理者用。アカウント管理、利用回数（初回/継続を別カラムで管理）の設定。
*   **InitialMeetingRoom (`/app/initial`)**: 初回面談（順次ヒアリング型）専用ページ。
*   **ContinuousMeetingRoom (`/app/continuous`)**: 継続面談（自由対話型）専用ページ。

## 3. コア機能：AI面談フローの完全分離
初回面談と2回目以降（継続使用）の面談を別のページ・別のロジックとして実装する。

### 3.1 初回面談ページ（順次ヒアリング型：`/app/initial`）
*   **目的**: 電子カルテ（SHIRP形式）のベースを完成させる。
*   **制御**: S(現状) → H(希望) → I(課題) → R(資源) の順に1つずつ質問を展開し、情報を埋めていく。
*   **通信方式**: ブラウザ録音(MediaRecorder) → Whisper(STT) → GPT-4o → TTS-1 の標準シーケンス。

### 3.2 継続面談ページ（自由対話型：`/app/continuous`）
*   **目的**: ユーザーが自由に話したいテーマを扱い、過去のカルテを上書き更新する。面談終了後にはキャリアに関する簡単なフィードバックを提供する。
*   **カルテ処理**: 初回で作成したカルテを事前に読み込んだ状態で対話を開始する。
*   **通信方式と切り替えUI（UserHomeでの選択）**:
    *   `UserHome` の「継続面談スタート」アクション時に、ユーザーに通信方式を選択させるUIを設ける。
    *   **オプションA: 通常モード**（Whisper + GPT-4o + TTS-1）
    *   **オプションB: ターンテイキングモード**（Realtime API等を利用。VADで無音・発話終了を検知して自然に相槌や割り込みを行う）。※UI上には将来的な課金を想定した表示の余地を残しておく。

## 4. キャリアカルテ（電子カルテ）仕様
カルテは以下の「3段構成」としてUIに表示し、PDF/CSV出力にも対応させる。

1.  **デモグラフィック（個人情報）**
    *   氏名、年齢、所属企業、職種など。
2.  **電子カルテ（SHIRP形式）**
    以下の項目をAIが順に聞き取り、まとめて各スロットに生成・格納する。
    *   **S (Satisfaction/現状)**
        *   組織適応
        *   自身への評価
        *   良好な人間関係
        *   #そのほかの現状
    *   **H (Hope/希望)**
        *   希望する収入
        *   希望する仕事内容
        *   希望する勤務形態
        *   #そのほかの希望
    *   **I (Issue/課題)**
        *   スキルの課題
        *   健康上の課題
        *   年齢の課題
        *   家庭の課題
        *   #そのほかの課題
    *   **R (Resource/資源)**
        *   強みとなる資格
        *   強みとなる経験
        *   強みとなる協力者
        *   強みとなる時間や資金
        *   #そのほかの強み
    *   **P (Plan/プラン)**
        *   S〜Rの情報を元に、AIが解決に向けたプランを生成する。
    *   **# (その他)**
        *   上記のS〜Pに当てはまらないが、ユーザーが話したい内容を自由に記録するための項目。
3.  **ユーザーアンケート結果**
    *   25問（5点満点）の回答から5つの因子（成長志向、課題解決志向、組織貢献志向、対人適応志向、情動反応傾向）のスコアを算出し、**レーダーチャート**として描画する。
    *   *※注: 正式な計算アルゴリズムを受領するまでは、フロントエンド（TypeScript）側に各項目の平均値を算出するなどの**ダミーの計算ロジックをハードコード**してUIを補完しておく。*

## 5. 管理・バックエンド仕様と将来展望
*   **利用回数の管理UI**: Admin画面のアカウント管理テーブルにおいて、**「初回面談残り回数」**と**「継続面談残り回数」**を明確に別の列として表示・編集できるUIとする。
*   **SSO・ディープリンク**: 将来的なVPS環境下では、外部サービスからのトークン付きURL（`/#/user/:userId?token=...`）によるSSOログインを実装する。
*   **コンサルタント向けシミュレータ**: 一般ユーザー（求職者）向けの開発完了後、コンサルタントが「患者（クライアント）」を相手に面談練習を行う機能の追加開発を予定している。
```